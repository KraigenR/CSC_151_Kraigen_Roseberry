package labs.example.loops;
public class WhileLoop {

    private void executeWhileLoop(){
        int counter = 1; // starting value of counter 
        int iterations = 0; //how many iterations the loop has gone through
        while (counter <= 10){ //this should allow it to run until it hits ten with ten being the last run.
            counter++; //this is a postfix adding 1 to the counter each time
            iterations++; // same postfix as above adding 1 
                             }
                                    
System.out.println("The loop executed " + iterations + " times. The Loop is now compelte.");} //prints after the while loop has run 10 times
//System.out.println("The loop is now complete."); //just adding to the first println.  
                            }
                        
            


